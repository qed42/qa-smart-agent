# ─────────────────────────────────────────────────────────────
# QA Smart Agent v1 — Python Dependencies
# Compatible: Windows 10+, macOS 12+, Linux (Ubuntu 20.04+)
# Python: 3.10+
# ─────────────────────────────────────────────────────────────

# HTTP — Figma API + Jira API
requests>=2.31.0

# AI providers — install all, use any
openai>=1.30.0
anthropic>=0.25.0
google-generativeai>=0.5.0

# Image processing — Figma frame compression
Pillow>=10.0.0
